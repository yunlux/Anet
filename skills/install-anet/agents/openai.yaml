interface:
  display_name: "Install Anet"
  short_description: "Install Anet or bootstrap one WSL Agent safely"
  default_prompt: "Use $install-anet to install Anet and, when explicitly authorized, bootstrap this WSL Agent onto the existing host Ahub or create the first one."
