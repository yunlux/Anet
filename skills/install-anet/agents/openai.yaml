interface:
  display_name: "Install Anet"
  short_description: "Install Anet on Linux or bootstrap WSL safely"
  default_prompt: "Use $install-anet to install and verify pinned Anet on Linux, selecting the persistent single-Ahub WSL bootstrap only when that setup is authorized."
