interface:
  display_name: "Install Anet"
  short_description: "Install and verify pinned Anet CLI plus MCP on Linux"
  default_prompt: "Use $install-anet to install and verify Anet with MCP on this Linux host."
